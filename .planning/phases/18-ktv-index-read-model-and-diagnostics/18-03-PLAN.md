---
phase: 18-ktv-index-read-model-and-diagnostics
plan: 03
type: execute
wave: 3
depends_on: [18-01-PLAN.md, 18-02-PLAN.md]
files_modified:
  - packages/domain/src/index.ts
  - apps/api/src/routes/song-search.ts
  - apps/api/src/server.ts
  - apps/api/src/test/song-search-routes.test.ts
  - apps/mobile-controller/src/App.tsx
  - apps/mobile-controller/src/App.css
  - apps/mobile-controller/src/i18n.tsx
  - apps/mobile-controller/src/test/controller.test.tsx
autonomous: true
requirements: [INDEX-02, INDEX-03, INDEX-04]
must_haves:
  truths:
    - "Mobile search includes KTV indexed results through the existing search endpoint."
    - "Indexed Mobile results are grouped by song and nested versions."
    - "Indexed Mobile results do not expose absolute NAS paths."
    - "Indexed Mobile queue buttons stay disabled in Phase 18 with the exact label 需同步入库后可点歌."
  artifacts:
    - path: "apps/api/src/routes/song-search.ts"
      provides: "Existing Mobile search route extended with indexed results"
      contains: "indexed"
    - path: "apps/mobile-controller/src/App.tsx"
      provides: "Mobile KTV indexed search rendering"
      contains: "indexed"
    - path: "apps/mobile-controller/src/test/controller.test.tsx"
      provides: "Mobile regression coverage for indexed source labels and disabled queue state"
      contains: "需同步入库后可点歌"
  key_links:
    - from: "apps/api/src/routes/song-search.ts"
      to: "apps/api/src/modules/ktv-index/ktv-index-read-repository.ts"
      via: "searchIndexedSongs"
      pattern: "indexed"
    - from: "apps/mobile-controller/src/App.tsx"
      to: "packages/domain/src/index.ts"
      via: "SongSearchResponse.indexed"
      pattern: "KTV索引"
---

<objective>
Extend existing Mobile search to include KTV indexed results with source labels, grouped versions, and safe disabled queue actions.

Purpose: Users should be able to search the real indexed library from the normal Mobile search surface, while Phase 18 still avoids queue-time catalog sync and playback mutations.
Output: `/rooms/:roomSlug/songs/search` includes an `indexed` section; Mobile renders indexed song groups and version rows without exposing paths or enabling queue buttons.
</objective>

<execution_context>
@/Users/shaolongfei/.codex/get-shit-done/workflows/execute-plan.md
@/Users/shaolongfei/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/18-ktv-index-read-model-and-diagnostics/18-CONTEXT.md
@.planning/phases/18-ktv-index-read-model-and-diagnostics/18-RESEARCH.md
@.planning/phases/18-ktv-index-read-model-and-diagnostics/18-UI-SPEC.md
@packages/domain/src/index.ts
@apps/api/src/routes/song-search.ts
@apps/api/src/server.ts
@apps/api/src/modules/ktv-index/ktv-index-read-repository.ts
@apps/mobile-controller/src/runtime/use-room-controller-runtime.ts
@apps/mobile-controller/src/App.tsx
@apps/mobile-controller/src/i18n.tsx
@apps/mobile-controller/src/App.css
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend the Mobile search route with indexed results</name>
  <files>apps/api/src/routes/song-search.ts, apps/api/src/server.ts, apps/api/src/test/song-search-routes.test.ts</files>
  <read_first>
    - packages/domain/src/index.ts
    - apps/api/src/routes/song-search.ts
    - apps/api/src/server.ts
    - apps/api/src/modules/ktv-index/ktv-index-read-repository.ts
    - apps/api/src/modules/catalog/repositories/song-repository.ts
    - packages/domain/src/index.ts
    - apps/api/src/test/song-search-routes.test.ts
  </read_first>
  <behavior>
    - Route path remains `/rooms/:roomSlug/songs/search`.
    - Formal catalog results remain in `local`.
    - Online补歌 behavior remains in `online`.
    - KTV indexed results are added under `indexed`.
    - Query limit is bounded and passed to the repository.
  </behavior>
  <action>
    In `packages/domain/src/index.ts`, make the Phase 18 indexed section required on `SongSearchResponse`:

    ```ts
    indexed: SongSearchIndexedSection;
    ```

    In `apps/api/src/routes/song-search.ts`, extend `SongSearchRouteDependencies`:

    ```ts
    ktvIndex?: Pick<KtvIndexReadRepository, "searchIndexedSongs">;
    ```

    Inside the existing route handler, after formal `records` are loaded, call:

    ```ts
    const indexedResults =
      (await dependencies.ktvIndex?.searchIndexedSongs({
        query,
        limit: Math.min(20, limit),
        versionsPerSong: 4
      })) ?? [];
    ```

    Add to `SongSearchResponse`:

    ```ts
    indexed: {
      status: indexedResults.length > 0 ? "available" : "unavailable",
      message: indexedResults.length > 0 ? "找到 KTV 索引结果" : "未找到 KTV 索引结果",
      results: indexedResults
    }
    ```

    In `apps/api/src/server.ts`, pass `ktvIndex: repositories.ktvIndex` to `registerSongSearchRoutes` when PostgreSQL repositories exist; in in-memory mode, omit it so `indexed.results` is empty.

    Update or add `apps/api/src/test/song-search-routes.test.ts` cases proving:

    - response includes `indexed.status`, `indexed.message`, and `indexed.results`;
    - route passes `query`, bounded `limit`, and `versionsPerSong: 4` to `searchIndexedSongs`;
    - indexed results include `sourceLabel: "KTV索引"`;
    - indexed version has `canQueue: false`, `queueState: "needs_catalog_sync"`, and `disabledLabel: "需同步入库后可点歌"`;
    - `JSON.stringify(response)` does not contain `/mnt/nas` or `file_path`.
  </action>
  <acceptance_criteria>
    - `rg -n "indexed: SongSearchIndexedSection" packages/domain/src/index.ts` exits 0.
    - `rg -n "ktvIndex\\?|searchIndexedSongs|indexed:" apps/api/src/routes/song-search.ts apps/api/src/server.ts` exits 0.
    - `rg -n "找到 KTV 索引结果|未找到 KTV 索引结果|versionsPerSong: 4" apps/api/src/routes/song-search.ts apps/api/src/test/song-search-routes.test.ts` exits 0.
    - `rg -n "needs_catalog_sync|需同步入库后可点歌|KTV索引" apps/api/src/test/song-search-routes.test.ts` exits 0.
    - `pnpm -F @home-ktv/api test -- src/test/song-search-routes.test.ts` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>pnpm -F @home-ktv/api test -- src/test/song-search-routes.test.ts</automated>
  </verify>
  <done>Existing Mobile search response carries formal, indexed, and online result sections safely.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render indexed search groups in Mobile</name>
  <files>apps/mobile-controller/src/App.tsx, apps/mobile-controller/src/i18n.tsx, apps/mobile-controller/src/App.css, apps/mobile-controller/src/test/controller.test.tsx</files>
  <read_first>
    - apps/mobile-controller/src/App.tsx
    - apps/mobile-controller/src/i18n.tsx
    - apps/mobile-controller/src/App.css
    - apps/mobile-controller/src/test/controller.test.tsx
    - packages/domain/src/index.ts
    - .planning/phases/18-ktv-index-read-model-and-diagnostics/18-UI-SPEC.md
  </read_first>
  <behavior>
    - Mobile search remains one existing search panel.
    - Indexed groups are visually distinct from formal local results.
    - Indexed version buttons are disabled and cannot call `requestAddSongVersion`.
    - No absolute path is rendered on Mobile.
  </behavior>
  <action>
    In `apps/mobile-controller/src/App.tsx`, after rendering `controller.songSearch?.local`, render `controller.songSearch.indexed.results`.

    For each indexed result, render:

    - title in `<strong>`;
    - artist in `<p>`;
    - a metadata row containing `KTV索引`, category, and `search.indexedVersionCount`;
    - version rows for `result.versions`.

    For each indexed version row, render:

    - `version.displayName`;
    - `version.sourceLabel`;
    - `version.extension`;
    - `formatFileSize(version.sizeBytes)` where null returns `未知大小`;
    - disabled `<button className="primary-button" type="button" disabled>{version.disabledLabel}</button>`.

    Do not call `controller.requestAddSongVersion` for indexed versions in Phase 18.

    Add helper:

    ```ts
    function formatFileSize(sizeBytes: number | null): string
    ```

    Use Chinese i18n keys:

    - `search.indexedTitle` -> `KTV 索引结果`
    - `search.indexedEmpty` -> `未找到 KTV 索引结果`
    - `search.indexedVersionCount` -> `{count} 个索引版本`
    - `search.unknownSize` -> `未知大小`

    In `apps/mobile-controller/src/App.css`, add classes:

    - `.indexed-panel`
    - `.indexed-result-row`
    - `.indexed-version-row`
    - `.indexed-source`

    Ensure long text wraps and chips/buttons do not overflow at mobile widths.
  </action>
  <acceptance_criteria>
    - `rg -n "indexed\\.results|indexed-panel|indexed-version-row|formatFileSize" apps/mobile-controller/src/App.tsx apps/mobile-controller/src/App.css` exits 0.
    - `rg -n "KTV 索引结果|未找到 KTV 索引结果|未知大小" apps/mobile-controller/src/i18n.tsx` exits 0.
    - `! rg -n "requestAddSongVersion\\([^\\n]*indexed|/mnt/nas|file_path" apps/mobile-controller/src/App.tsx apps/mobile-controller/src/test/controller.test.tsx` exits 0.
    - `pnpm -F @home-ktv/mobile-controller test -- src/test/controller.test.tsx` exits 0.
    - `pnpm -F @home-ktv/mobile-controller typecheck` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>pnpm -F @home-ktv/mobile-controller test -- src/test/controller.test.tsx &amp;&amp; pnpm -F @home-ktv/mobile-controller typecheck</automated>
  </verify>
  <done>Mobile search visibly includes KTV indexed results without enabling unsafe queue actions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add cross-surface regression and typecheck coverage</name>
  <files>apps/api/src/test/song-search-routes.test.ts, apps/mobile-controller/src/test/controller.test.tsx, apps/admin/src/test/song-catalog.test.tsx</files>
  <read_first>
    - apps/api/src/test/song-search-routes.test.ts
    - apps/mobile-controller/src/test/controller.test.tsx
    - apps/admin/src/test/song-catalog.test.tsx
    - packages/domain/src/index.ts
    - .planning/phases/18-ktv-index-read-model-and-diagnostics/18-CONTEXT.md
  </read_first>
  <behavior>
    - Tests prove INDEX-02 through INDEX-04 across API and UI.
    - Tests prove formal catalog search behavior remains present.
    - Tests prove indexed results remain source-labeled and disabled until Phase 19.
  </behavior>
  <action>
    Add final regression assertions:

    In `apps/api/src/test/song-search-routes.test.ts`:

    - formal `local` result still includes a `versions[0].assetId`;
    - indexed result includes `indexedSongId` and `indexedAssetId`;
    - indexed result JSON excludes `filePath`, `file_path`, and `/mnt/nas`;
    - missing indexed repository produces `indexed.status === "unavailable"` and `indexed.results.length === 0`.

    In `apps/mobile-controller/src/test/controller.test.tsx`:

    - indexed result renders `KTV索引`;
    - disabled button renders `需同步入库后可点歌`;
    - clicking the disabled indexed button does not call an add-queue command mock;
    - formal local result can still show an enabled add button when `canQueue` is true.

    In `apps/admin/src/test/song-catalog.test.tsx`:

    - Songs view still renders formal catalog list controls;
    - KTV diagnostics section renders in the same Songs view;
    - NAS sample fixture with `status: "unmapped"` renders `未映射`, the unmapped count, and the sample row/message;
    - no top-level navigation item named `KTV` is expected.
  </action>
  <acceptance_criteria>
    - `rg -n "filePath|file_path|/mnt/nas|indexedSongId|indexedAssetId" apps/api/src/test/song-search-routes.test.ts` exits 0.
    - `rg -n "需同步入库后可点歌|KTV索引" apps/mobile-controller/src/test/controller.test.tsx` exits 0.
    - `rg -n "KTV 索引诊断|正式曲库|formal|unmapped|未映射" apps/admin/src/test/song-catalog.test.tsx` exits 0.
    - `pnpm -F @home-ktv/api test -- src/test/ktv-index-read-repository.test.ts src/test/admin-ktv-index-routes.test.ts src/test/song-search-routes.test.ts` exits 0.
    - `pnpm -F @home-ktv/admin test -- src/test/song-catalog.test.tsx` exits 0.
    - `pnpm -F @home-ktv/mobile-controller test -- src/test/controller.test.tsx` exits 0.
    - `pnpm -F @home-ktv/api typecheck && pnpm -F @home-ktv/admin typecheck && pnpm -F @home-ktv/mobile-controller typecheck` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>pnpm -F @home-ktv/api test -- src/test/ktv-index-read-repository.test.ts src/test/admin-ktv-index-routes.test.ts src/test/song-search-routes.test.ts &amp;&amp; pnpm -F @home-ktv/admin test -- src/test/song-catalog.test.tsx &amp;&amp; pnpm -F @home-ktv/mobile-controller test -- src/test/controller.test.tsx &amp;&amp; pnpm -F @home-ktv/api typecheck &amp;&amp; pnpm -F @home-ktv/admin typecheck &amp;&amp; pnpm -F @home-ktv/mobile-controller typecheck</automated>
  </verify>
  <done>Phase 18 search/source-label behavior is covered across API, Admin, and Mobile.</done>
</task>

</tasks>

<verification>
Run:
- `pnpm -F @home-ktv/api test -- src/test/ktv-index-read-repository.test.ts src/test/admin-ktv-index-routes.test.ts src/test/song-search-routes.test.ts`
- `pnpm -F @home-ktv/admin test -- src/test/song-catalog.test.tsx`
- `pnpm -F @home-ktv/mobile-controller test -- src/test/controller.test.tsx`
- `pnpm -F @home-ktv/api typecheck`
- `pnpm -F @home-ktv/admin typecheck`
- `pnpm -F @home-ktv/mobile-controller typecheck`
</verification>

<success_criteria>
- INDEX-02 Mobile search only consumes active indexed assets from the repository.
- INDEX-03 Mobile search can find indexed songs through title/artist/pinyin/initial/category metadata.
- INDEX-04 Mobile distinguishes formal catalog results from KTV indexed results.
- Indexed Mobile results preserve song/asset identity for Phase 19 but do not call queue commands.
- Mobile UI does not expose absolute NAS paths.
</success_criteria>

<output>
After completion, create `.planning/phases/18-ktv-index-read-model-and-diagnostics/18-03-SUMMARY.md`.
</output>
