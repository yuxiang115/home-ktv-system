---
phase: 19-search-and-queue-time-catalog-sync
plan: 04
type: execute
wave: 4
depends_on: [19-01-PLAN.md, 19-02-PLAN.md, 19-03-PLAN.md]
files_modified:
  - apps/api/src/routes/song-search.ts
  - apps/api/src/routes/admin-catalog.ts
  - apps/api/src/routes/admin-ktv-index.ts
  - apps/api/src/test/song-search-routes.test.ts
  - apps/api/src/test/ktv-catalog-sync-service.test.ts
  - apps/api/src/test/indexed-queue-command.test.ts
  - apps/api/src/test/realtime-room-sync.test.ts
  - packages/domain/src/index.ts
  - apps/admin/src/api/client.ts
  - apps/admin/src/songs/SongCatalogView.tsx
  - apps/admin/src/songs/use-song-catalog-runtime.ts
  - apps/admin/src/i18n.tsx
  - apps/admin/src/test/song-catalog.test.tsx
  - apps/mobile-controller/src/test/controller.test.tsx
autonomous: true
requirements: [SYNC-01, SYNC-02, SYNC-03, SYNC-04]
must_haves:
  truths:
    - "All SYNC requirements are covered by automated regression tests."
    - "Search can show already-synced indexed assets as 已点 while keeping them in the KTV index section."
    - "Admin can inspect KTV source identity for canonical records created from indexed assets."
    - "Realtime room snapshots and queue operations work with synced real songs using canonical ids."
  artifacts:
    - path: "apps/api/src/test/indexed-queue-command.test.ts"
      provides: "Queue-time sync and command integration regression coverage"
      contains: "indexedAssetId"
    - path: "apps/api/src/test/realtime-room-sync.test.ts"
      provides: "Realtime snapshot coverage for synced indexed queue entries"
      contains: "song-ktv"
    - path: "apps/admin/src/test/song-catalog.test.tsx"
      provides: "Admin source identity visibility coverage"
      contains: "ktv_song_assets.id"
    - path: "apps/mobile-controller/src/test/controller.test.tsx"
      provides: "Mobile indexed point-song UI regression coverage"
      contains: "正在加入"
  key_links:
    - from: "apps/api/src/test/indexed-queue-command.test.ts"
      to: "apps/api/src/modules/catalog/ktv-catalog-sync-service.ts"
      via: "syncIndexedAsset"
      pattern: "source_records"
    - from: "apps/api/src/test/realtime-room-sync.test.ts"
      to: "apps/api/src/routes/control-commands.ts"
      via: "indexed add-queue-entry snapshot broadcast"
      pattern: "room.control.snapshot|indexedAssetId"
---

<objective>
Close Phase 19 regression coverage and Admin traceability for queue-time synced KTV indexed songs.

Purpose: The phase changes cross search, sync, command execution, realtime snapshots, Mobile UI, and Admin diagnostics. The final plan verifies those seams together and exposes source identity for operators.
Output: search-state refinements, Admin source inspection, integration/regression tests, and final typecheck/test commands.
</objective>

<execution_context>
@/Users/shaolongfei/.codex/get-shit-done/workflows/execute-plan.md
@/Users/shaolongfei/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/19-search-and-queue-time-catalog-sync/19-CONTEXT.md
@.planning/phases/19-search-and-queue-time-catalog-sync/19-RESEARCH.md
@packages/domain/src/index.ts
@apps/api/src/routes/song-search.ts
@apps/api/src/routes/control-commands.ts
@apps/api/src/modules/catalog/ktv-catalog-sync-service.ts
@apps/api/src/modules/rooms/build-control-snapshot.ts
@apps/admin/src/songs/SongCatalogView.tsx
@apps/mobile-controller/src/App.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Show synced indexed queue state in search</name>
  <files>apps/api/src/routes/song-search.ts, apps/api/src/test/song-search-routes.test.ts</files>
  <read_first>
    - apps/api/src/routes/song-search.ts
    - apps/api/src/modules/ktv-index/ktv-index-read-repository.ts
    - apps/api/src/modules/playback/repositories/queue-entry-repository.ts
    - apps/api/src/test/song-search-routes.test.ts
    - packages/domain/src/index.ts
    - .planning/phases/19-search-and-queue-time-catalog-sync/19-CONTEXT.md
  </read_first>
  <behavior>
    - Search response identifies indexed assets whose canonical Song is already in the effective queue.
    - The result remains under `indexed.results`.
    - Mobile receives `queueState: "queued"` and can render `已点`.
    - Mobile response still omits NAS paths.
  </behavior>
  <action>
    Add a small repository or route helper in `apps/api/src/routes/song-search.ts` that maps effective canonical queue entries back to KTV indexed source identity via `source_records`.

    If implemented as a dependency, add:

    ```ts
    indexedSources?: {
      findIndexedAssetIdsForCanonicalAssets(assetIds: readonly string[]): Promise<string[]>;
    };
    ```

    If implemented inside a repository, use this SQL shape:

    ```sql
    SELECT provider_item_id
    FROM source_records
    WHERE provider = 'ktv-index'
      AND provider_item_id IS NOT NULL
      AND asset_id = ANY($1::text[])
    ```

    In the search route:

    1. Load effective queue with `queueEntries.listEffectiveQueue(room.id)`.
    2. Collect queued canonical asset ids.
    3. Resolve queued indexed asset ids through `source_records`.
    4. Pass those ids as `queuedIndexedAssetIds` to `searchIndexedSongs`.

    Update tests proving:

    - when `queue_entries.asset_id = 'asset-ktv-ktv-asset-1'` and `source_records.provider_item_id = 'ktv-asset-1'`, indexed search version `ktv-asset-1` returns `queueState: "queued"`;
    - the same response keeps the result under `indexed.results`;
    - `JSON.stringify(response)` does not contain `/mnt/nas`, `filePath`, or `file_path`.
  </action>
  <acceptance_criteria>
    - `rg -n "findIndexedAssetIdsForCanonicalAssets|source_records|provider = 'ktv-index'|queuedIndexedAssetIds" apps/api/src/routes/song-search.ts apps/api/src/test/song-search-routes.test.ts` exits 0.
    - `rg -n "queueState.*queued|indexed\\.results" apps/api/src/test/song-search-routes.test.ts` exits 0.
    - `! rg -n "/mnt/nas|filePath|file_path" apps/api/src/test/song-search-routes.test.ts` exits 0.
    - `pnpm -F @home-ktv/api test -- src/test/song-search-routes.test.ts` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>pnpm -F @home-ktv/api test -- src/test/song-search-routes.test.ts</automated>
  </verify>
  <done>Search reflects queued indexed assets without leaving the KTV index section or exposing paths.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add Admin source identity inspection for synced canonical records</name>
  <files>packages/domain/src/index.ts, apps/api/src/routes/admin-catalog.ts, apps/api/src/routes/admin-ktv-index.ts, apps/admin/src/api/client.ts, apps/admin/src/songs/use-song-catalog-runtime.ts, apps/admin/src/songs/SongCatalogView.tsx, apps/admin/src/i18n.tsx, apps/admin/src/test/song-catalog.test.tsx</files>
  <read_first>
    - apps/admin/src/api/client.ts
    - apps/admin/src/songs/use-song-catalog-runtime.ts
    - apps/admin/src/songs/SongCatalogView.tsx
    - apps/admin/src/i18n.tsx
    - apps/admin/src/test/song-catalog.test.tsx
    - packages/domain/src/index.ts
    - apps/api/src/routes/admin-catalog.ts
    - apps/api/src/routes/admin-ktv-index.ts
    - .planning/phases/19-search-and-queue-time-catalog-sync/19-CONTEXT.md
  </read_first>
  <behavior>
    - Admin source identity stays inside Songs/KTV diagnostics.
    - Admin can see canonical records created from the KTV index.
    - Admin can see `ktv_songs.id`, `ktv_song_assets.id`, and original `file_path`.
    - Mobile remains path-safe; this Admin-only detail must not be imported into Mobile components.
  </behavior>
  <action>
    Extend the existing Admin Songs diagnostics or formal song detail data path to include KTV source identity for canonical assets.

    Preferred API shape in the Admin KTV diagnostics response or formal song detail:

    ```ts
    interface KtvIndexSyncedSourceRecord {
      songId: string;
      assetId: string;
      indexedSongId: string;
      indexedAssetId: string;
      filePath: string;
      title: string;
      artistName: string;
      category: string;
      parseConfidence: number | null;
    }
    ```

    The API query should read `source_records` rows where `provider = 'ktv-index'` and `raw_meta` contains `indexedSongId`, `indexedAssetId`, `filePath`, `category`, and `parseConfidence`.

    In `apps/admin/src/songs/SongCatalogView.tsx`, render an operator section titled `KTV 同步来源` inside the Songs workspace. It must show labels:

    - `ktv_songs.id`
    - `ktv_song_assets.id`
    - `文件路径`
    - `解析置信度`

    Add i18n keys for Chinese and English.

    Add tests proving:

    - `KTV 同步来源` renders in Songs;
    - `ktv_song_assets.id` and `ktv-asset-1` render;
    - `/mnt/nas/KTV歌曲/周杰伦/七里香.mkv` renders in Admin;
    - the Mobile controller test suite still has no `/mnt/nas` rendering.
  </action>
  <acceptance_criteria>
    - `rg -n "KTV 同步来源|ktv_song_assets\\.id|ktv_songs\\.id|解析置信度" apps/admin/src/songs/SongCatalogView.tsx apps/admin/src/i18n.tsx apps/admin/src/test/song-catalog.test.tsx` exits 0.
    - `rg -n "provider.*ktv-index|raw_meta|parseConfidence|indexedAssetId" apps/admin/src/api/client.ts apps/admin/src/songs/use-song-catalog-runtime.ts apps/admin/src/test/song-catalog.test.tsx apps/api/src/routes/admin-ktv-index.ts apps/api/src/routes/admin-catalog.ts` exits 0.
    - `rg -n "/mnt/nas/KTV歌曲/周杰伦/七里香\\.mkv" apps/admin/src/test/song-catalog.test.tsx` exits 0.
    - `! rg -n "/mnt/nas|filePath|file_path" apps/mobile-controller/src/App.tsx apps/mobile-controller/src/test/controller.test.tsx` exits 0.
    - `pnpm -F @home-ktv/admin test -- src/test/song-catalog.test.tsx` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>pnpm -F @home-ktv/admin test -- src/test/song-catalog.test.tsx</automated>
  </verify>
  <done>Operators can trace synced canonical records back to KTV index source rows inside Admin Songs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Prove queue/realtime operations for synced indexed songs</name>
  <files>apps/api/src/test/indexed-queue-command.test.ts, apps/api/src/test/realtime-room-sync.test.ts, apps/mobile-controller/src/test/controller.test.tsx</files>
  <read_first>
    - apps/api/src/test/indexed-queue-command.test.ts
    - apps/api/src/test/realtime-room-sync.test.ts
    - apps/api/src/routes/control-commands.ts
    - apps/api/src/modules/rooms/build-control-snapshot.ts
    - apps/api/src/modules/playback/session-command-service.ts
    - apps/mobile-controller/src/test/controller.test.tsx
    - .planning/phases/19-search-and-queue-time-catalog-sync/19-CONTEXT.md
  </read_first>
  <behavior>
    - Add, duplicate confirmation, promote, delete, undo delete, skip current, and room snapshot refresh continue working for synced real songs.
    - Realtime snapshots contain canonical queue preview ids.
    - No snapshot or queue entry type exposes raw `ktv_*` ids.
  </behavior>
  <action>
    Extend backend tests so a synced indexed song is queued and then exercised through existing operations.

    Required cases:

    1. Add indexed asset:

       - request body has `indexedAssetId: "ktv-asset-1"`;
       - accepted snapshot queue row has `songId: "song-ktv-ktv-song-1"` and `assetId: "asset-ktv-ktv-asset-1"`;
       - queue repository row has no `indexedAssetId` field.

    2. Promote/delete/undo:

       - queue two indexed assets;
       - call `promote-queue-entry` on the second;
       - call `delete-queue-entry` on a queued entry;
       - call `undo-delete-queue-entry`;
       - assert returned snapshots still show canonical ids and correct queue order.

    3. Skip current:

       - add an indexed current song and a second song;
       - call `skip-current` with `confirmSkip: true`;
       - assert snapshot advances to the next canonical queue entry.

    4. Realtime:

       - in `apps/api/src/test/realtime-room-sync.test.ts`, prove accepted indexed add broadcasts `room.control.snapshot`;
       - assert broadcast payload contains `song-ktv-` and `asset-ktv-`;
       - assert broadcast payload does not contain `ktv_song_assets`, `file_path`, or `/mnt/nas`.

    Extend Mobile tests to prove the existing duplicate confirmation dialog works for indexed queued state:

    - search fixture has indexed version with `queueState: "queued"`;
    - tapping `已点` opens the existing duplicate confirmation;
    - confirming sends `indexedAssetId`.
  </action>
  <acceptance_criteria>
    - `rg -n "promote-queue-entry|delete-queue-entry|undo-delete-queue-entry|skip-current" apps/api/src/test/indexed-queue-command.test.ts` exits 0.
    - `rg -n "room\\.control\\.snapshot|song-ktv-|asset-ktv-|indexedAssetId" apps/api/src/test/realtime-room-sync.test.ts apps/api/src/test/indexed-queue-command.test.ts` exits 0.
    - `! rg -n "ktv_song_assets|file_path|/mnt/nas" apps/api/src/test/realtime-room-sync.test.ts apps/api/src/test/indexed-queue-command.test.ts` exits 0.
    - `rg -n "duplicate|已点|indexedAssetId" apps/mobile-controller/src/test/controller.test.tsx` exits 0.
    - `pnpm -F @home-ktv/api test -- src/test/indexed-queue-command.test.ts src/test/realtime-room-sync.test.ts` exits 0.
    - `pnpm -F @home-ktv/mobile-controller test -- src/test/controller.test.tsx` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>pnpm -F @home-ktv/api test -- src/test/indexed-queue-command.test.ts src/test/realtime-room-sync.test.ts</automated>
    <automated>pnpm -F @home-ktv/mobile-controller test -- src/test/controller.test.tsx</automated>
  </verify>
  <done>Queue operations and realtime snapshots are proven for synced indexed songs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Run final Phase 19 verification commands</name>
  <files>apps/api/src/test/ktv-catalog-sync-service.test.ts, apps/api/src/test/indexed-queue-command.test.ts, apps/api/src/test/song-search-routes.test.ts, apps/api/src/test/realtime-room-sync.test.ts, apps/admin/src/test/song-catalog.test.tsx, apps/mobile-controller/src/test/controller.test.tsx</files>
  <read_first>
    - .planning/phases/19-search-and-queue-time-catalog-sync/19-CONTEXT.md
    - .planning/phases/19-search-and-queue-time-catalog-sync/19-RESEARCH.md
    - .planning/REQUIREMENTS.md
    - apps/api/src/test/ktv-catalog-sync-service.test.ts
    - apps/api/src/test/indexed-queue-command.test.ts
    - apps/api/src/test/song-search-routes.test.ts
    - apps/api/src/test/realtime-room-sync.test.ts
    - apps/admin/src/test/song-catalog.test.tsx
    - apps/mobile-controller/src/test/controller.test.tsx
  </read_first>
  <behavior>
    - Every Phase 19 requirement has automated evidence.
    - Domain/API/Admin/Mobile typechecks pass after all cross-package contract changes.
    - The final test set can be copied into UAT/verification.
  </behavior>
  <action>
    Run these commands and fix any failure within Phase 19 scope:

    ```bash
    pnpm -F @home-ktv/api test -- src/test/ktv-catalog-sync-service.test.ts src/test/indexed-queue-command.test.ts src/test/song-search-routes.test.ts src/test/realtime-room-sync.test.ts
    pnpm -F @home-ktv/admin test -- src/test/song-catalog.test.tsx
    pnpm -F @home-ktv/mobile-controller test -- src/test/controller.test.tsx
    pnpm -F @home-ktv/domain build
    pnpm -F @home-ktv/api typecheck
    pnpm -F @home-ktv/admin typecheck
    pnpm -F @home-ktv/mobile-controller typecheck
    ```

    If any test needs a fixture update, keep it limited to Phase 19 behavior:

    - indexed queue payloads;
    - canonical `song-ktv-*` / `asset-ktv-*` ids;
    - source record identity;
    - Admin source inspection;
    - Mobile `点歌` / `正在加入...` / `已点` / `索引已失效` / `文件不可读`.
  </action>
  <acceptance_criteria>
    - `pnpm -F @home-ktv/api test -- src/test/ktv-catalog-sync-service.test.ts src/test/indexed-queue-command.test.ts src/test/song-search-routes.test.ts src/test/realtime-room-sync.test.ts` exits 0.
    - `pnpm -F @home-ktv/admin test -- src/test/song-catalog.test.tsx` exits 0.
    - `pnpm -F @home-ktv/mobile-controller test -- src/test/controller.test.tsx` exits 0.
    - `pnpm -F @home-ktv/domain build` exits 0.
    - `pnpm -F @home-ktv/api typecheck` exits 0.
    - `pnpm -F @home-ktv/admin typecheck` exits 0.
    - `pnpm -F @home-ktv/mobile-controller typecheck` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>pnpm -F @home-ktv/api test -- src/test/ktv-catalog-sync-service.test.ts src/test/indexed-queue-command.test.ts src/test/song-search-routes.test.ts src/test/realtime-room-sync.test.ts</automated>
    <automated>pnpm -F @home-ktv/admin test -- src/test/song-catalog.test.tsx</automated>
    <automated>pnpm -F @home-ktv/mobile-controller test -- src/test/controller.test.tsx</automated>
    <automated>pnpm -F @home-ktv/domain build &amp;&amp; pnpm -F @home-ktv/api typecheck &amp;&amp; pnpm -F @home-ktv/admin typecheck &amp;&amp; pnpm -F @home-ktv/mobile-controller typecheck</automated>
  </verify>
  <done>Phase 19 has automated evidence for search, sync, queue, realtime, Admin, and Mobile behavior.</done>
</task>

</tasks>

<verification>
- `pnpm -F @home-ktv/api test -- src/test/ktv-catalog-sync-service.test.ts src/test/indexed-queue-command.test.ts src/test/song-search-routes.test.ts src/test/realtime-room-sync.test.ts`
- `pnpm -F @home-ktv/admin test -- src/test/song-catalog.test.tsx`
- `pnpm -F @home-ktv/mobile-controller test -- src/test/controller.test.tsx`
- `pnpm -F @home-ktv/domain build && pnpm -F @home-ktv/api typecheck && pnpm -F @home-ktv/admin typecheck && pnpm -F @home-ktv/mobile-controller typecheck`
</verification>
