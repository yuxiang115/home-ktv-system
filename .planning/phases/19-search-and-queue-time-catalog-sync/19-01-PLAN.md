---
phase: 19-search-and-queue-time-catalog-sync
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/domain/src/index.ts
  - apps/api/src/modules/ktv-index/ktv-index-read-repository.ts
  - apps/api/src/routes/song-search.ts
  - apps/api/src/test/song-search-routes.test.ts
  - apps/mobile-controller/src/api/client.ts
  - apps/mobile-controller/src/runtime/use-room-controller-runtime.ts
  - apps/mobile-controller/src/App.tsx
  - apps/mobile-controller/src/App.css
  - apps/mobile-controller/src/i18n.tsx
  - apps/mobile-controller/src/test/controller.test.tsx
autonomous: true
requirements: [SYNC-01, SYNC-04]
must_haves:
  truths:
    - "Mobile indexed version rows show a normal 点歌 button for queueable indexed results."
    - "Mobile sends only indexedAssetId for indexed queue requests and never sends indexed metadata or NAS paths."
    - "Indexed queued state remains visible in the KTV 索引结果 section as 已点."
    - "Stale or unreadable indexed versions remain visible but disabled with 索引已失效 or 文件不可读."
  artifacts:
    - path: "packages/domain/src/index.ts"
      provides: "Shared indexed queue state contract"
      contains: "SongSearchIndexedQueueState"
    - path: "apps/mobile-controller/src/App.tsx"
      provides: "Indexed result version buttons and inline pending state"
      contains: "正在加入"
    - path: "apps/mobile-controller/src/test/controller.test.tsx"
      provides: "Mobile indexed queue UI and payload regression coverage"
      contains: "indexedAssetId"
  key_links:
    - from: "apps/mobile-controller/src/App.tsx"
      to: "apps/mobile-controller/src/runtime/use-room-controller-runtime.ts"
      via: "requestAddIndexedAsset"
      pattern: "indexedAssetId"
    - from: "apps/api/src/routes/song-search.ts"
      to: "packages/domain/src/index.ts"
      via: "SongSearchResponse.indexed"
      pattern: "not_queued|queued|source_missing|file_unreadable"
---

<objective>
Update the indexed search contract and Mobile search UI so KTV indexed results can present queueable, queued, stale, and unreadable states.

Purpose: Phase 19 should make indexed results feel directly point-song capable from Mobile while preserving server-authoritative queue state and keeping NAS paths off Mobile.
Output: shared contract changes, search response mapping, Mobile indexed buttons, inline pending state, duplicate-confirm compatible runtime methods, and focused tests.
</objective>

<execution_context>
@/Users/shaolongfei/.codex/get-shit-done/workflows/execute-plan.md
@/Users/shaolongfei/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/19-search-and-queue-time-catalog-sync/19-CONTEXT.md
@.planning/phases/19-search-and-queue-time-catalog-sync/19-RESEARCH.md
@.planning/phases/18-ktv-index-read-model-and-diagnostics/18-CONTEXT.md
@packages/domain/src/index.ts
@apps/api/src/routes/song-search.ts
@apps/api/src/modules/ktv-index/ktv-index-read-repository.ts
@apps/mobile-controller/src/runtime/use-room-controller-runtime.ts
@apps/mobile-controller/src/App.tsx
@apps/mobile-controller/src/i18n.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend indexed search queue states</name>
  <files>packages/domain/src/index.ts, apps/api/src/modules/ktv-index/ktv-index-read-repository.ts, apps/api/src/routes/song-search.ts, apps/api/src/test/song-search-routes.test.ts</files>
  <read_first>
    - packages/domain/src/index.ts
    - apps/api/src/modules/ktv-index/ktv-index-read-repository.ts
    - apps/api/src/routes/song-search.ts
    - apps/api/src/test/song-search-routes.test.ts
    - .planning/phases/19-search-and-queue-time-catalog-sync/19-CONTEXT.md
    - .planning/phases/19-search-and-queue-time-catalog-sync/19-RESEARCH.md
  </read_first>
  <behavior>
    - Active indexed versions are queueable by default in Phase 19.
    - Queued indexed state is represented without moving the result to the local section.
    - Disabled indexed states are explicit and Chinese-labeled.
    - Mobile-facing indexed JSON still omits `filePath`, `file_path`, and `/mnt/nas`.
  </behavior>
  <action>
    In `packages/domain/src/index.ts`, replace the Phase 18 indexed queue state with:

    ```ts
    export type SongSearchIndexedQueueState = "not_queued" | "queued" | "source_missing" | "file_unreadable";
    ```

    Update `SongSearchIndexedVersionOption` so it has:

    ```ts
    queueState: SongSearchIndexedQueueState;
    canQueue: boolean;
    disabledLabel: string | null;
    ```

    Do not add `filePath`, `sourceUri`, `rawMeta`, or full KTV metadata to the Mobile indexed version contract.

    In `apps/api/src/modules/ktv-index/ktv-index-read-repository.ts`, extend `SearchKtvIndexedSongsInput` with:

    ```ts
    queuedIndexedAssetIds?: readonly string[];
    unreadableIndexedAssetIds?: readonly string[];
    ```

    Map each active indexed asset as:

    - if `unreadableIndexedAssetIds` contains the asset id: `queueState: "file_unreadable"`, `canQueue: false`, `disabledLabel: "文件不可读"`;
    - else if `queuedIndexedAssetIds` contains the asset id: `queueState: "queued"`, `canQueue: true`, `disabledLabel: null`;
    - otherwise: `queueState: "not_queued"`, `canQueue: true`, `disabledLabel: null`.

    In `apps/api/src/routes/song-search.ts`, keep the existing `/rooms/:roomSlug/songs/search` route and continue returning `indexed`. For this plan, pass empty arrays for `queuedIndexedAssetIds` and `unreadableIndexedAssetIds` until Plan 19-02/19-03 introduces source identity lookups:

    ```ts
    queuedIndexedAssetIds: [],
    unreadableIndexedAssetIds: []
    ```

    Update `apps/api/src/test/song-search-routes.test.ts` so active indexed results assert:

    ```ts
    queueState: "not_queued",
    canQueue: true,
    disabledLabel: null
    ```

    Add a repository or route-level fixture proving `queuedIndexedAssetIds: ["ktv-asset-sunny-mkv"]` maps that version to `queueState: "queued"` and `canQueue: true`.
  </action>
  <acceptance_criteria>
    - `rg -n "SongSearchIndexedQueueState = .*not_queued.*queued.*source_missing.*file_unreadable" packages/domain/src/index.ts` exits 0.
    - `rg -n "canQueue: boolean|disabledLabel: string \\| null" packages/domain/src/index.ts` exits 0.
    - `rg -n "queuedIndexedAssetIds|unreadableIndexedAssetIds|file_unreadable|文件不可读" apps/api/src/modules/ktv-index/ktv-index-read-repository.ts apps/api/src/test/song-search-routes.test.ts` exits 0.
    - `! rg -n "filePath|file_path|/mnt/nas" apps/mobile-controller/src/App.tsx apps/mobile-controller/src/test/controller.test.tsx` exits 0.
    - `pnpm -F @home-ktv/api test -- src/test/song-search-routes.test.ts src/test/ktv-index-read-repository.test.ts` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>pnpm -F @home-ktv/api test -- src/test/song-search-routes.test.ts src/test/ktv-index-read-repository.test.ts</automated>
  </verify>
  <done>Indexed search contracts can represent queueable, queued, stale, and unreadable version states without exposing paths.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire Mobile indexed queue actions and inline states</name>
  <files>apps/mobile-controller/src/api/client.ts, apps/mobile-controller/src/runtime/use-room-controller-runtime.ts, apps/mobile-controller/src/App.tsx, apps/mobile-controller/src/App.css, apps/mobile-controller/src/i18n.tsx, apps/mobile-controller/src/test/controller.test.tsx</files>
  <read_first>
    - apps/mobile-controller/src/api/client.ts
    - apps/mobile-controller/src/runtime/use-room-controller-runtime.ts
    - apps/mobile-controller/src/App.tsx
    - apps/mobile-controller/src/App.css
    - apps/mobile-controller/src/i18n.tsx
    - apps/mobile-controller/src/test/controller.test.tsx
    - packages/domain/src/index.ts
    - .planning/phases/19-search-and-queue-time-catalog-sync/19-CONTEXT.md
  </read_first>
  <behavior>
    - Queueable indexed versions show `点歌`.
    - The clicked indexed version button shows `正在加入...` while the command is in flight.
    - Queued indexed versions stay in the KTV index section and show `已点`.
    - Disabled indexed versions show `索引已失效` or `文件不可读`.
    - Indexed queue payload contains `indexedAssetId` only, plus existing command envelope fields.
  </behavior>
  <action>
    In `apps/mobile-controller/src/api/client.ts`, extend `addQueueEntry` input so callers can pass either canonical IDs or `indexedAssetId`:

    ```ts
    export async function addQueueEntry(
      input: CommandBaseInput & (
        | { songId: string; assetId?: string; indexedAssetId?: never }
        | { indexedAssetId: string; songId?: never; assetId?: never }
      )
    )
    ```

    Send canonical body fields only when `songId` is present, and send:

    ```ts
    { indexedAssetId: input.indexedAssetId }
    ```

    only when `indexedAssetId` is present.

    In `apps/mobile-controller/src/runtime/use-room-controller-runtime.ts`:

    - add `pendingIndexedAssetId: string | null` runtime state;
    - add `requestAddIndexedAsset(indexedAssetId: string, title: string, queueState: SongSearchIndexedQueueState): void`;
    - if `queueState === "queued"`, open duplicate confirmation with an indexed selection payload;
    - otherwise call `addQueueEntry({ ...input, indexedAssetId })`;
    - clear `pendingIndexedAssetId` on success and failure;
    - after success, refresh the control snapshot and rerun the current search query so the indexed section can show `已点`.

    Update duplicate confirmation state from canonical-only:

    ```ts
    { songId: string; assetId: string; title: string }
    ```

    to a union:

    ```ts
    | { kind: "canonical"; songId: string; assetId: string; title: string }
    | { kind: "indexed"; indexedAssetId: string; title: string }
    ```

    In `apps/mobile-controller/src/App.tsx`, for each indexed version:

    - compute `isPending = controller.pendingIndexedAssetId === version.indexedAssetId`;
    - button label is `正在加入...` when pending;
    - button label is `已点` when `version.queueState === "queued"` and not pending;
    - button label is `点歌` when `version.canQueue` and not queued;
    - button label is `version.disabledLabel` when `!version.canQueue`;
    - call `controller.requestAddIndexedAsset(version.indexedAssetId, result.title, version.queueState)` only when `version.canQueue && !isPending`.

    In `apps/mobile-controller/src/i18n.tsx`, add Chinese and English keys:

    - `button.addIndexed`: `点歌`
    - `button.addingIndexed`: `正在加入...`
    - `search.indexedQueued`: `已点`
    - `search.indexedStale`: `索引已失效`
    - `search.indexedUnreadable`: `文件不可读`

    In `apps/mobile-controller/src/App.css`, ensure `.indexed-version-row > button` keeps a stable width and centered text on narrow screens.

    Update `apps/mobile-controller/src/test/controller.test.tsx` with tests proving:

    - queueable indexed result renders `点歌`;
    - clicking it sends `POST /rooms/living-room/commands/add-queue-entry` with `indexedAssetId`;
    - request body does not include `songId`, `assetId`, `/mnt/nas`, `filePath`, or `file_path`;
    - pending state renders `正在加入...`;
    - queued state renders `已点`;
    - disabled states render `索引已失效` and `文件不可读`.
  </action>
  <acceptance_criteria>
    - `rg -n "indexedAssetId" apps/mobile-controller/src/api/client.ts apps/mobile-controller/src/runtime/use-room-controller-runtime.ts apps/mobile-controller/src/App.tsx apps/mobile-controller/src/test/controller.test.tsx` exits 0.
    - `rg -n "requestAddIndexedAsset|pendingIndexedAssetId" apps/mobile-controller/src/runtime/use-room-controller-runtime.ts apps/mobile-controller/src/App.tsx` exits 0.
    - `rg -n "点歌|正在加入|已点|索引已失效|文件不可读" apps/mobile-controller/src/i18n.tsx apps/mobile-controller/src/test/controller.test.tsx` exits 0.
    - `! rg -n "indexedAssetId.*songId|indexedAssetId.*assetId|filePath|file_path|/mnt/nas" apps/mobile-controller/src/test/controller.test.tsx` exits 0.
    - `pnpm -F @home-ktv/mobile-controller test -- src/test/controller.test.tsx` exits 0.
    - `pnpm -F @home-ktv/mobile-controller typecheck` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>pnpm -F @home-ktv/mobile-controller test -- src/test/controller.test.tsx</automated>
    <automated>pnpm -F @home-ktv/mobile-controller typecheck</automated>
  </verify>
  <done>Mobile can initiate indexed queue commands with direct point-song UI and correct inline feedback.</done>
</task>

</tasks>

<verification>
- `pnpm -F @home-ktv/api test -- src/test/song-search-routes.test.ts src/test/ktv-index-read-repository.test.ts`
- `pnpm -F @home-ktv/mobile-controller test -- src/test/controller.test.tsx`
- `pnpm -F @home-ktv/mobile-controller typecheck`
</verification>
